# SKOJ - N-Queen

- [문제출처](https://skoj.site/problem/10119 'N-Queen')

## 풀이

### 접근

- `백트래킹`

### 설계

- 행(`depth`)마다 열을 하나씩 시도하며 이미 놓인 퀸들(`pos`)과 같은 열·대각선인지 검사, 걸리는 퀸이 있으면 `break`로 해당 열을 가지치기
- 모든 퀸과 충돌 없이 통과한 열에서만 `pos`에 좌표를 추가해 재귀 호출하고, 반환 후 `pos.pop()`으로 상태를 복원해 백트래킹 구현
