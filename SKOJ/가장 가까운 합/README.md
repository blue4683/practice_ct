# SKOJ - 가장 가까운 합

- [문제출처](https://skoj.site/problem/10258 '가장 가까운 합')

## 풀이

### 접근

- `완전탐색`

### 설계

- 서로 다른 두 원소의 모든 조합 `(i, j)`를 순회하며 합 `k`에 대해 `|t - k|`를 계산하는 O(N²) 완전탐색으로 최소 차이 탐색
- 차이가 기존 최솟값과 같을 경우 `min(result, k)`로 더 작은 합을 선택
