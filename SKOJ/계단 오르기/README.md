# SKOJ - 계단 오르기

- [문제출처](https://skoj.site/problem/10288 '계단 오르기')

## 풀이

### 접근

- `DP`

### 설계

- `dp[0] = 1`에서 시작해, 각 칸 `i`에서 `i+1`칸과 `i+2`칸으로 값을 밀어 넣는(forward) 방식으로 도달 방법 수를 누적
- 두 경우 모두 갱신 시마다 `MOD`로 나눈 나머지를 유지해 최대 `N=1,000,000`까지 오버플로우 없이 처리
