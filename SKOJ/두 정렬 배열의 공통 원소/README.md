# SKOJ - 두 정렬 배열의 공통 원소

- [문제출처](https://skoj.site/problem/10199 '두 정렬 배열의 공통 원소')

## 풀이

### 접근

- `투 포인터`

### 설계

- 두 배열이 이미 오름차순 정렬되어 있다는 점을 이용해 포인터 `l`, `r`을 동시에 옮기며 공통 원소 탐색
  - 값이 다르면 더 작은 쪽 포인터만 전진, 같으면 결과를 세고 양쪽 모두 전진
- 한쪽 배열의 인덱스가 끝(`a` 또는 `b`)에 도달하면 남은 포인터만 전진시켜 인덱스 초과 방지
