# SKOJ - 라면은 순서가 생명이다

- [문제출처](https://skoj.site/problem/10004 '라면은 순서가 생명이다')

## 풀이

### 접근

- `위상 정렬`

### 설계

- 위상 정렬 도중 진입차수가 0인 정점이 큐에 두 개 이상 동시에 존재하면 순서가 유일하지 않다는 뜻이므로 그 즉시 `'NO'`로 종료
- 큐가 빌 때까지 정상적으로 위상 정렬을 마쳤을 때 방문한 정점 수가 `N`과 같아야 사이클이 없다는 뜻이므로, 그 경우에만 `'YES'`로 판정
