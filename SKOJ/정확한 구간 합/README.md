# SKOJ - 정확한 구간 합

- [문제출처](https://skoj.site/problem/10200 '정확한 구간 합')

## 풀이

### 접근

- `투 포인터`

### 설계

- 모든 원소가 양수라는 조건을 이용해 `r`을 오른쪽으로 넓히고, 합이 `s`를 넘으면 `v > s`인 동안 계속 `l`을 줄이는 표준 슬라이딩 윈도우로 구현
  - 매 `r`마다 윈도우 정리가 끝난 뒤 `v == s` 여부를 확인하므로, `r`이 마지막 인덱스에 도달한 경우에도 구간 누락 없이 처리됨
  