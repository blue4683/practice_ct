# SKOJ - 최댓값 꺼내기

- [문제출처](https://skoj.site/problem/10270 '최댓값 꺼내기')

## 풀이

### 접근

- `힙(최대 힙)`

### 설계

- Python `heapq`가 최소 힙만 지원하는 점을 이용해 값을 음수로 바꿔 `push`하고, 꺼낼 때 다시 부호를 뒤집어 최대 힙처럼 동작시킴
- `0` 명령이 들어오면 힙이 비어 있는지 먼저 확인해 비었으면 `0`, 아니면 최댓값을 `pop`해 출력
