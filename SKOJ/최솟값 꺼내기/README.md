# SKOJ - 최솟값 꺼내기

- [문제출처](https://skoj.site/problem/10272 '최솟값 꺼내기')

## 풀이

### 접근

- `힙(최소 힙)`

### 설계

- Python `heapq`가 기본적으로 최소 힙이라는 점을 그대로 이용해 값을 부호 변환 없이 `push`
- `0` 명령이 들어오면 힙이 비어 있는지 먼저 확인해 비었으면 `0`, 아니면 최솟값을 `pop`해 출력
